name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: My-IT-Guy-Organization/My-IT-Guy-DevOps/.github/workflows/reusable-netlify-deploy.yml@main
    with:
      publish-dir: "."
      # build-command: "npm run build"
    secrets:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
