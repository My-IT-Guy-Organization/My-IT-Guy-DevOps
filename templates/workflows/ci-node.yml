name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: My-IT-Guy-Organization/My-IT-Guy-DevOps/.github/workflows/reusable-node-ci.yml@main
    with:
      node-version: "20"
