name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: My-IT-Guy-Organization/My-IT-Guy-DevOps/.github/workflows/reusable-python-ci.yml@main
    with:
      python-version: "3.12"
